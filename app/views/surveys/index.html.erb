<h1>This are all of our surveys</h1>
<h3>Pick one...</h3>
<table>
  <tr>
    <th>Title</th>
  </tr>
  <% @surveys.each do |s| %>
  <tr>
    <td><%= link_to s.title, survey_path(s) %></td>
  </tr>
  <% end %>
</table>